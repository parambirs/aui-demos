(function (global, factory) {
    if (typeof define === 'function' && define.amd) {
        define(['exports', 'module', './jquery', './internal/deprecation', './contains', './internal/globalize'], factory);
    } else if (typeof exports !== 'undefined' && typeof module !== 'undefined') {
        factory(exports, module, require('./jquery'), require('./internal/deprecation'), require('./contains'), require('./internal/globalize'));
    } else {
        var mod = {
            exports: {}
        };
        factory(mod.exports, mod, global.$, global.deprecation, global.contains, global.globalize);
        global.include = mod.exports;
    }
})(this, function (exports, module, _jquery, _internalDeprecation, _contains, _internalGlobalize) {
    'use strict';

    function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

    var _$ = _interopRequireDefault(_jquery);

    var _contains2 = _interopRequireDefault(_contains);

    var _globalize = _interopRequireDefault(_internalGlobalize);

    var included = [];

    function include(url) {
        if (!(0, _contains2['default'])(included, url)) {
            included.push(url);
            var s = document.createElement('script');
            s.src = url;
            (0, _$['default'])('body').append(s);
        }
    }

    var include = (0, _internalDeprecation.fn)(include, 'include', {
        sinceVersion: '5.9.0',
        extraInfo: 'No alternative will be provided. Use a proper module loader instead.'
    });

    (0, _globalize['default'])('include', include);

    module.exports = include;
});
//# sourceMappingURL=../../js/aui/include.js.map