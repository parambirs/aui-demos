(function (global, factory) {
    if (typeof define === 'function' && define.amd) {
        define(['exports', 'module', './underscore', 'backbone'], factory);
    } else if (typeof exports !== 'undefined' && typeof module !== 'undefined') {
        factory(exports, module, require('./underscore'), require('backbone'));
    } else {
        var mod = {
            exports: {}
        };
        factory(mod.exports, mod, global._, global.Backbone);
        global.backbone = mod.exports;
    }
})(this, function (exports, module, _underscore, _backbone) {
    'use strict';

    function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

    var _2 = _interopRequireDefault(_underscore);

    var _Backbone = _interopRequireDefault(_backbone);

    if (!window.Backbone) {
        window.Backbone = _Backbone['default'];
    }

    module.exports = window.Backbone;
});
//# sourceMappingURL=../../js/aui/backbone.js.map