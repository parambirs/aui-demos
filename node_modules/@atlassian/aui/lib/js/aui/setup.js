(function (global, factory) {
    if (typeof define === 'function' && define.amd) {
        define(['exports', './jquery', './internal/globalize', './populate-parameters', './version'], factory);
    } else if (typeof exports !== 'undefined') {
        factory(exports, require('./jquery'), require('./internal/globalize'), require('./populate-parameters'), require('./version'));
    } else {
        var mod = {
            exports: {}
        };
        factory(mod.exports, global.$, global.globalize, global.populateParameters, global.version);
        global.setup = mod.exports;
    }
})(this, function (exports, _jquery, _internalGlobalize, _populateParameters, _version) {
    // Global setup that used to be in the main AJS namespace file.

    'use strict';

    function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

    var _$ = _interopRequireDefault(_jquery);

    var _globalize = _interopRequireDefault(_internalGlobalize);

    var _populateParameters2 = _interopRequireDefault(_populateParameters);

    var _version2 = _interopRequireDefault(_version);

    // Set the version.
    (0, _$['default'])(function () {
        var $body = (0, _$['default'])('body');

        if (!$body.data('auiVersion')) {
            $body.attr('data-aui-version', _version2['default']);
        }

        (0, _populateParameters2['default'])();
    });

    // Setting Traditional to handle our default param serialisation.
    // See http://api.jquery.com/jQuery.param/ for more information.
    _$['default'].ajaxSettings.traditional = true;
    (0, _globalize['default'])('$', _$['default']);
});
//# sourceMappingURL=../../js/aui/setup.js.map