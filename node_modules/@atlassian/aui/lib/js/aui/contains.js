(function (global, factory) {
  if (typeof define === 'function' && define.amd) {
    define(['exports', 'module', './internal/globalize', './index-of'], factory);
  } else if (typeof exports !== 'undefined' && typeof module !== 'undefined') {
    factory(exports, module, require('./internal/globalize'), require('./index-of'));
  } else {
    var mod = {
      exports: {}
    };
    factory(mod.exports, mod, global.globalize, global.indexOf);
    global.contains = mod.exports;
  }
})(this, function (exports, module, _internalGlobalize, _indexOf) {
  'use strict';

  function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

  var _globalize = _interopRequireDefault(_internalGlobalize);

  var _indexOf2 = _interopRequireDefault(_indexOf);

  /**
   * Looks for an element inside the array.
   *
   * @param {Array} array The array being searched.
   * @param {Array} item The current item.
   *
   * @return {Boolean}
   */
  function contains(array, item) {
    return (0, _indexOf2['default'])(array, item) > -1;
  }

  (0, _globalize['default'])('contains', contains);

  module.exports = contains;
});
//# sourceMappingURL=../../js/aui/contains.js.map